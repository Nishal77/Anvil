NEXT_PUBLIC_API_URL=http://localhost:8080
# Optional — unset hides the job detail page's "View trace" link
# entirely (EG-3, PRD §17.1) rather than rendering a dead one. Point
# at the Grafana instance from docker-compose.yml's observability
# stack (grafana service, published on :3001 locally).
NEXT_PUBLIC_GRAFANA_URL=http://localhost:3001
