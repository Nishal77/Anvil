// Package telemetry configures structured logging for every other package
// in the control plane. It has no internal dependencies (CLAUDE.md PK5) and
// exposes NewLogger as its only entry point.
package telemetry
